akka.persistence.testkit {

  messages {
    serialize-messages = true
    assert-timeout = 1s
    assert-poll-interval = 100millis
  }

  snapshots {
    serialize-messages = true
    assert-timeout = 1s
    assert-poll-interval = 100millis
  }

}
