akka.actor.serialize-messages = on
akka.actor.serialize-creators = on

akka.checker.config.disabled-checks = [
  enabled-transports, 
  hostname,
  power-user-settings,
  auto-down,
  cluster-failure-detector]
