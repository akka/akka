package akka.http.model

import org.scalatest.WordSpecLike

class HttpMessageSpec extends WordSpecLike {
  "HttpMessage" should {
    "implement header[T]" in pending
    "implement withDefaultHeaders" in pending
  }
}
